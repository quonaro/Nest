# Example with positional arguments
greet(name: str, message: str):
    > desc: Приветствие с позиционными аргументами
    > script: |
        #!/bin/sh
        echo "Привет {{name}}, {{message}}!"

# Example with named arguments (using ! prefix)
build(!target|t: str = "x86_64", !release|r: bool = false):
    > desc: Собрать проект с помощью Cargo (именованные аргументы)
    > cwd: ./app
    > env: .env
    > env: BUILD_TIME={{now}}
    > script: |
        #!/bin/sh
        echo "🔨 Building {{target}} (release={{release}})"
        cargo build --target "{{target}}" ${release:+--release}

# Example with mixed positional and named arguments
copy(source: str, !destination|d: str, !overwrite|o: bool = false):
    > desc: Копировать файл (позиционный + именованные аргументы)
    > script: |
        #!/bin/sh
        if [ "{{overwrite}}" = "true" ]; then
            cp -f "{{source}}" "{{destination}}"
            echo "Файл {{source}} скопирован в {{destination}} (с перезаписью)"
        else
            cp "{{source}}" "{{destination}}"
            echo "Файл {{source}} скопирован в {{destination}}"
        fi

dev:
    > desc: Инструменты для локальной разработки

    default(hot: bool = false):
        > desc: Запустить dev-сервер
        > env: .env.local
        > env: NODE_ENV=development
        > script: |
            #!/bin/sh
            if [ "$hot" = "true" ]; then
                nodemon src/index.js
            else
                node src/index.js
            fi

    lint(fix|f: bool = false):
        > desc: Проверить и исправить код
        > script: |
            #!/bin/sh
            eslint src/ ${fix:+--fix}

db:
    > desc: Управление базой данных

    default():
        > desc: Справка по командам БД
        > script: |
            #!/bin/sh
            echo "Доступные команды:"
            echo "  nest db up     — накатить миграции"
            echo "  nest db down   — откатить миграции"

    up():
        > desc: Применить миграции
        > env: DATABASE_URL=postgresql://localhost/dev
        > script: ./scripts/migrate_up.sh

    down(force|f: bool = false):
        > desc: Откатить последнюю миграцию
        > script: |
            #!/bin/sh
            if [ "$force" = "true" ]; then
                ./migrate down --force
            else
                ./migrate down
            fi

deploy:
    > desc: Деплой приложения

    prod(
        version: str,
        !confirm|c: bool = false,
        !retries|r: num = 3,
        !tags|t: arr = []
    ):
        > desc: Задеплоить в production (позиционный version + именованные опции)
        > env: .env.prod
        > env: DEPLOYER={{user}}
        > script: |
            #!/bin/sh
            if [ "{{confirm}}" != "true" ]; then
                echo "🛑 Используй --confirm или -c"
                exit 1
            fi
            cmd="./deploy.sh {{version}} --retries {{retries}}"
            for tag in $(echo "{{tags}}" | tr ',' ' '); do
                cmd="$cmd --tag $tag"
            done
            eval "$cmd"

    staging(version: str):
        > desc: Деплой в staging-среду (позиционный аргумент)
        > env: .env.staging
        > script: |
            #!/bin/sh
            ./deploy.sh "{{version}}" --env staging